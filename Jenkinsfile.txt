pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                UiPathPack (
outputPath: '${WORKSPACE}\\Output', projectJsonPath: '${WORKSPACE}\\project.json', version: CurrentVersion())
            }
        }
stage('Deploy') {
            steps {
		 UiPathPack (
                outputPath: '${WORKSPACE}\\Output', projectJsonPath: '${WORKSPACE}\\project.json', version: CurrentVersion())
            }
        }
    }
}